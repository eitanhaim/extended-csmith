-*- mode: Text -*-

      ExtendedCsmith --- an extension of Csmith

This extension adds two features that are not supported by its last version:
      1) Use of recursive functions;
      2) Use of common libraries.

